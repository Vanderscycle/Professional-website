apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: professional-web-tls
spec:
  dnsNames:
    - me.vandersleyen.dev
  secretName: professional-website-tls
  issuerRef:
    name: letsencrypt-cluster-issuer-non-prod
    kind: ClusterIssuer
